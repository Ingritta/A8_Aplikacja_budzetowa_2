#include "Date.h"
/*
void Date::setWrittenDate(int newWrittenDate) {
    if (newWrittenDate >= 0)
        writtenDate = newWrittenDate;
}

void Date::setDateFromOS(int newDateFromOS) {
    if (newDateFromOS >= 0)
        dateFromOS = newDateFromOS;
}

int Date::getWrittenDate() {
    return writtenDate;
}

int Date::getDateFromOS() {
    return dateFromOS;
}
*/
